# Bridge config: TurtleBot3 Waffle (Tier 2 — requires ros-${ROS_DISTRO}-turtlebot3-description)
#
# Demonstrates Tier 2 URDF resolution: the bridge resolves the model name
# "turtlebot3_waffle" to the installed ROS package path via resolve_model().
#
# Prerequisites:
#   apt install ros-${ROS_DISTRO}-turtlebot3-description
#
# Usage:
#   ros2 launch pybullet_fleet_ros tb3_demo.launch.py model:=waffle
#   ros2 launch pybullet_fleet_ros tb3_demo.launch.py model:=waffle gui:=true

simulation:
  gui: true
  physics: false
  target_rtf: 5.0

robots:
  - name: tb3_0
    urdf_path: "turtlebot3_waffle"
    pose: [0.0, 0.0, 0.01]
    yaw: 0.0
    motion_mode: "differential"
    max_linear_vel: 0.26
    max_linear_accel: 2.5
    max_angular_vel: 1.82
    max_angular_accel: 10.0
