# .gitleaks.toml — Gitleaks configuration for PyBulletFleet
# https://github.com/gitleaks/gitleaks#configuration

title = "PyBulletFleet gitleaks config"

[allowlist]
  description = "Allowlisted patterns and paths"

  # Only the specific design doc that contains example grep patterns (not actual secrets)
  paths = [
    '''docs/design/pypi-release/agent\.spec\.md$''',
  ]
