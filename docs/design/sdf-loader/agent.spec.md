# SDF Loader — Agent Specification

## Requirements

### Functional

- `SimObject.from_sdf()` classmethod — `p.loadSDF()` wrapper, returns `List[SimObject]`
- `load_rmf_world()` function — OBJ mesh directory loader for rmf_demos environments
- `resolve_model()` alias in `robot_models.py` — URDF/SDF dual resolution
- Tests: `tests/test_sdf_loader.py`
- Example: `examples/basics/sdf_demo.py`

### Non-Functional

- SDF 読み込み後のオブジェクトは既存 SimObject と完全に同じ振る舞い (collision, attachment, etc.)
- `load_rmf_world()` は 100+ OBJ ファイルでも 5 秒以内に読み込み
- No new dependencies (PyBullet の `p.loadSDF()` + 標準ライブラリのみ)

## Constraints

- PyBullet `p.loadSDF()` は `<world>`, `<include>`, `model://` 非対応
- DAE mesh は visual only (collision shape 非対応)
- `from_sdf()` は `SimObject.__init__(body_id=...)` を使う — 新しい初期化パスは作らない
- `_shared_shapes` キャッシュは SDF では使わない (PyBullet が内部で shape を管理)

## Approach

`p.loadSDF()` が返す body_id を SimObject でラップ。既存の `__init__()` が object_id 割り当て、
CollisionMode 設定、sim_core 登録を自動で行うので、factory method はシンプル。

OBJ 環境ローダーは既存の `from_mesh()` を活用。

## Design

### Component Table

| Component | File | Change |
|-----------|------|--------|
| `SimObject.from_sdf()` | `pybullet_fleet/sim_object.py` | **New classmethod** |
| `load_rmf_world()` | `pybullet_fleet/world_loader.py` | **New file** |
| `resolve_model()` | `pybullet_fleet/robot_models.py` | Alias for `resolve_model()` |
| Public API | `pybullet_fleet/__init__.py` | Export `load_rmf_world` |
| Tests | `tests/test_sdf_loader.py` | **New file** |
| Example | `examples/basics/sdf_demo.py` | **New file** |

### Code Patterns

#### SimObject.from_sdf()

```python
# pybullet_fleet/sim_object.py — new classmethod

@classmethod
def from_sdf(
    cls,
    sdf_path: str,
    sim_core=None,
    collision_mode: CollisionMode = CollisionMode.NORMAL_3D,
    global_scaling: float = 1.0,
    name_prefix: Optional[str] = None,
    pickable: bool = False,
    use_fixed_base: bool = True,
) -> List["SimObject"]:
    """Load an SDF file and wrap each body in a SimObject.

    Uses PyBullet's ``p.loadSDF()`` to load the file and creates a SimObject
    for each body. All objects are auto-registered to ``sim_core`` if provided.

    Note: PyBullet's SDF loader does NOT support ``<world>`` tags,
    ``<include>`` tags, or ``model://`` URIs. Use for individual SDF models
    (e.g., pybullet_data's kiva_shelf, wsg50_gripper).

    For rmf_demos environments (which use ``<world>`` + ``<include>``),
    use :func:`pybullet_fleet.world_loader.load_rmf_world` instead.

    Args:
        sdf_path: Path to SDF file (resolved via resolve_model() if relative)
        sim_core: Simulation core for registration
        collision_mode: Collision detection mode for all loaded objects
        global_scaling: Uniform scale factor
        name_prefix: Prefix for auto-generated names (default: SDF filename stem)
        pickable: Whether objects can be picked up
        use_fixed_base: If True, fix base position (mass=0, kinematic)

    Returns:
        List of SimObject instances (one per body in the SDF)

    Example::

        shelves = SimObject.from_sdf("kiva_shelf/model.sdf", sim_core=sim)
        for shelf in shelves:
            print(f"{shelf.name}: {shelf.get_pose()}")
    """
    from pybullet_fleet.robot_models import resolve_model

    resolved_path = resolve_model(sdf_path)
    pid = sim_core._client if sim_core is not None else 0

    body_ids = p.loadSDF(
        resolved_path,
        globalScaling=global_scaling,
        physicsClientId=pid,
    )

    if not body_ids:
        raise RuntimeError(f"SDF loaded 0 bodies from: {resolved_path}")

    # Determine name prefix from filename if not provided
    if name_prefix is None:
        name_prefix = Path(resolved_path).stem

    objects = []
    for i, bid in enumerate(body_ids):
        # Try to get model name from PyBullet
        body_info = p.getBodyInfo(bid, physicsClientId=pid)
        body_name = body_info[1].decode("utf-8") if body_info[1] else f"{name_prefix}_{i}"

        if use_fixed_base:
            # Set mass to 0 for static/kinematic behavior
            p.changeDynamics(bid, -1, mass=0.0, physicsClientId=pid)

        obj = cls(
            body_id=bid,
            sim_core=sim_core,
            pickable=pickable,
            collision_mode=collision_mode,
            name=body_name,
        )
        objects.append(obj)

    logger.info(f"Loaded {len(objects)} objects from SDF: {resolved_path}")
    return objects
```

#### world_loader.py (new file)

```python
# pybullet_fleet/world_loader.py
"""Environment loading utilities for simulation worlds.

Provides helpers to load pre-built environment assets (OBJ meshes, SDF models)
into a PyBulletFleet simulation. Primary use case: loading rmf_demos environments
generated by rmf_building_map_tools.
"""

import glob
import os
from pathlib import Path
from typing import List, Optional

from pybullet_fleet.geometry import Pose
from pybullet_fleet.logging_utils import get_lazy_logger
from pybullet_fleet.sim_object import SimObject, ShapeParams, SimObjectSpawnParams
from pybullet_fleet.types import CollisionMode

logger = get_lazy_logger(__name__)


def load_rmf_world(
    mesh_dir: str,
    sim_core=None,
    collision_mode: CollisionMode = CollisionMode.NORMAL_3D,
    mesh_scale: Optional[List[float]] = None,
    rgba_color: Optional[List[float]] = None,
    pattern: str = "*.obj",
) -> List[SimObject]:
    """Load OBJ mesh files from a directory as static SimObjects.

    Designed for rmf_demos environments where ``rmf_building_map_tools``
    generates OBJ meshes from building.yaml floor plans.

    Workflow::

        # 1. Generate meshes from rmf_demos building map
        ros2 run rmf_building_map_tools building_map_generator \\
            office.building.yaml output_dir/

        # 2. Load into PyBulletFleet
        objects = load_rmf_world("output_dir/meshes/", sim_core=sim)

    Args:
        mesh_dir: Directory containing OBJ mesh files
        sim_core: Simulation core for registration
        collision_mode: Collision mode for environment objects
        mesh_scale: Optional scale override [sx, sy, sz] (default: [1,1,1])
        rgba_color: Optional color override [r,g,b,a] (default: per-mesh)
        pattern: Glob pattern for mesh files (default: "*.obj")

    Returns:
        List of SimObject instances (one per mesh file)
    """
    mesh_dir = os.path.expanduser(mesh_dir)
    if not os.path.isdir(mesh_dir):
        raise FileNotFoundError(f"Mesh directory not found: {mesh_dir}")

    mesh_files = sorted(glob.glob(os.path.join(mesh_dir, pattern)))
    if not mesh_files:
        logger.warning(lambda: f"No {pattern} files found in {mesh_dir}")
        return []

    scale = mesh_scale or [1.0, 1.0, 1.0]
    objects = []

    for mesh_path in mesh_files:
        name = Path(mesh_path).stem

        visual = ShapeParams(
            shape_type="mesh",
            mesh_path=mesh_path,
            mesh_scale=scale,
            rgba_color=rgba_color or [0.7, 0.7, 0.7, 1.0],
        )
        collision = ShapeParams(
            shape_type="mesh",
            mesh_path=mesh_path,
            mesh_scale=scale,
        )

        # Origin placement: rmf_building_map_tools bakes world coordinates
        # into OBJ vertex data, so no per-mesh pose offset is needed.
        obj = SimObject.from_mesh(
            visual_params=visual,
            collision_params=collision,
            initial_pose=Pose.from_xyz(0, 0, 0),
            mass=0.0,  # static
            sim_core=sim_core,
            collision_mode=collision_mode,
            name=name,
        )
        objects.append(obj)

    logger.info(lambda: f"Loaded {len(objects)} environment objects from {mesh_dir}")
    return objects
```

#### robot_models.py — resolve_model alias

```python
# pybullet_fleet/robot_models.py — addition

def resolve_model(name_or_path: str) -> str:
    """Resolve a model name or path to an absolute file path.

    Supports both URDF and SDF files. Alias for resolve_model() with
    extended SDF awareness.

    Args:
        name_or_path: Model name ("kiva_shelf") or path ("robots/arm.urdf")

    Returns:
        Absolute path to the model file
    """
    return resolve_model(name_or_path)
```

#### __init__.py additions

```python
from pybullet_fleet.world_loader import load_rmf_world
from pybullet_fleet.robot_models import resolve_model
```

### Test Patterns

```python
# tests/test_sdf_loader.py

def test_from_sdf_kiva_shelf(sim_core_headless):
    """Load pybullet_data kiva_shelf SDF."""
    objects = SimObject.from_sdf("kiva_shelf/model.sdf", sim_core=sim_core_headless)
    assert len(objects) >= 1
    assert all(isinstance(o, SimObject) for o in objects)
    assert all(o.object_id >= 0 for o in objects)  # registered
    assert all(o.name is not None for o in objects)

def test_from_sdf_global_scaling(sim_core_headless):
    """Global scaling parameter works."""
    objects = SimObject.from_sdf(
        "kiva_shelf/model.sdf", sim_core=sim_core_headless, global_scaling=2.0
    )
    assert len(objects) >= 1

def test_from_sdf_invalid_path():
    """Invalid SDF path raises error."""
    with pytest.raises((FileNotFoundError, RuntimeError)):
        SimObject.from_sdf("nonexistent.sdf")

def test_from_sdf_object_spawned_event(sim_core_headless):
    """object_spawned event fires for each SDF body."""
    spawned = []
    sim_core_headless.events.on("object_spawned", lambda obj: spawned.append(obj))
    objects = SimObject.from_sdf("kiva_shelf/model.sdf", sim_core=sim_core_headless)
    assert len(spawned) == len(objects)

def test_load_rmf_world(sim_core_headless, tmp_path):
    """load_rmf_world loads OBJ files from directory."""
    # Create dummy OBJ file
    obj_content = "v 0 0 0\nv 1 0 0\nv 0 1 0\nf 1 2 3\n"
    (tmp_path / "wall.obj").write_text(obj_content)
    (tmp_path / "floor.obj").write_text(obj_content)

    objects = load_rmf_world(str(tmp_path), sim_core=sim_core_headless)
    assert len(objects) == 2
    assert all(o.mass == 0.0 for o in objects)  # static

def test_load_rmf_world_empty_dir(tmp_path):
    """Empty directory returns empty list."""
    objects = load_rmf_world(str(tmp_path))
    assert objects == []

def test_resolve_model_sdf():
    """resolve_model resolves SDF paths."""
    path = resolve_model("kiva_shelf")
    assert path.endswith(".sdf")
    assert os.path.exists(path)
```

## File References

Files the plan agent MUST read before planning:

- `pybullet_fleet/sim_object.py:259-340` — `SimObject.__init__()` (body_id wrapping)
- `pybullet_fleet/sim_object.py:587-660` — `from_mesh()` (factory pattern reference)
- `pybullet_fleet/sim_object.py:665-710` — `from_params()` / `from_dict()` (factory pattern)
- `pybullet_fleet/robot_models.py` — `resolve_model()`, search path resolution
- `pybullet_fleet/__init__.py` — current exports
- `examples/models/model_catalog_demo.py:204-211` — existing raw `p.loadSDF()` usage
- `pybullet_fleet/types.py` — `CollisionMode` enum

## Success Criteria

- [ ] `SimObject.from_sdf("kiva_shelf/model.sdf", sim_core=sim)` works
- [ ] Returns `List[SimObject]` with correct lifecycle management
- [ ] `global_scaling` parameter works correctly
- [ ] `resolve_model("kiva_shelf")` resolves to SDF path
- [ ] `load_rmf_world(mesh_dir, sim_core)` loads OBJ files as static SimObjects
- [ ] `object_spawned` event fires for each SDF body (EventBus dependency)
- [ ] All existing tests pass without modification
- [ ] `make verify` passes
